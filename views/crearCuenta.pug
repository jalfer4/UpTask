<!DOCTYPE html>
html(lang="en")
    head 
       include partials/header
    body.login 

        .contenedor-formulario    
            h1 crea tu cuenta en UpTast

            if locals.mensajes
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo 
                    label(for="email") E-mail: 
                    input(type="email" name="email" value= email placeholder="Tu e-mail")
                .campo 
                    label(for="password") Contraseña
                    input(type="password" name="password" value= password placeholder="Tu contraseña")
                .campo.enviar 
                    input.boton(type="submit" value="Crear cuenta")
                .campo
                    a(href="/iniciar-sesion") Volver a iniciar sessión
    
    Script(src="/dist/bundle.js")