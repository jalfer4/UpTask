doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario  
            h1= nombrePagina    
            p Agregar tu e-mail para restablecer la contraseña 
            if locals.mensajes
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
            form.caja-login(method="POST" action="/reestablecer")
                .campo 
                    label(for="email") E-mail: 
                    input(type="email" name="email" value= email placeholder="Tu e-mail")
                .campo.enviar 
                    input.boton(type="submit" value="Reestablecer contraseña")
                .campo.acciones
                    a(href="/iniciar-sesion") Volver a iniciar sesión